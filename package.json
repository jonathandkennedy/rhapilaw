{
  "name": "rhapilaw-landers",
  "version": "1.0.0",
  "private": true,
  "description": "Robert Hindin & Associates — car accident landers for 14 LA County cities, EN/ES.",
  "scripts": {
    "build": "node build.js",
    "qa": "node build.js && node qa.js",
    "serve": "node build.js && python3 -m http.server 8080 --directory dist"
  },
  "engines": { "node": ">=18" }
}
